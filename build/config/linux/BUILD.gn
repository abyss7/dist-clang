config("base") {
  defines = [
    "OS_LINUX",
  ]

  ldflags = [
    # Can't use this flag with ASan: http://llvm.org/bugs/show_bug.cgi?id=21341
    # "--no-undefined",
    "-rdynamic",      # for |backtrace()|
  ]
}
