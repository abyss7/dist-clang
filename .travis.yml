os: [linux, osx]

dist: xenial
osx_image: xcode10.1

language: cpp
compiler: clang

addons:
  apt:
    packages:
      - liblzma-dev
      - ninja-build
      - zlib1g-dev
  homebrew:
    packages:
      - ninja
    update: true

script: ./build/travis.sh
