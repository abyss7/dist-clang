executable("clang") {
  sources = [ "clang_main.cc" ]
  deps += [
    ":client",
    "//src/base:constants",
    "//src/base:logging",
    "//src/third_party/protobuf:protobuf",
  ]
}

source_set("client") {
  sources = [
    "clang.cc",
    "clang.h",
  ]

  deps += [
    ":command",
    "//src/base:base",
    "//src/base:logging",
    "//src/net:net",
    "//src/proto:proto",
  ]
}

# NOTICE: Uses custom libclang ldflags, thus, have to be shared library.
shared_library("command") {
  sources = [
    "command.cc",
    "command.h",
  ]

  configs += [ "//build/config:libclang" ]

  deps += [
    "//src/base:base",
    "//src/base:logging",
    "//src/proto:proto",
    "//src/third_party/protobuf:protobuf",
  ]

  all_dependent_configs = [ "//build/config:libclang_includes" ]
}
