#pragma once

// This attributes indicate the function's or method's thread-safety.
// All thread-unsafe functions should be called before thread-safe ones.
#define THREAD_SAFE
#define THREAD_UNSAFE

#define WEAK_PTR
