package dist_clang.proto;

message Compiler {
  required string path        = 1;
  optional string version     = 2;  // a version is used in a cache, and for
                                    // remote compilation.
}
